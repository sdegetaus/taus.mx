interface SocialKey {
  name: string;
  icon: string;
  url: URL;
}

interface Languages {
  en: string;
  es: string;
}

interface Email {
  name: string;
  domain: string;
}

interface Attribution {
  name: string;
  text: string;
  url: URL;
}

interface Technology {
  title: string;
  name: string;
}
